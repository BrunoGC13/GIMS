// === Libs ===
const path = require('path');
const { createConstructor } = require("../vars/constructor");
require('dotenv').config({
    path: path.resolve(__dirname, '../../.env')
});

// === Main ===
async function checkWarns(player) {

}